# Base URL of the FastAPI backend, including the API prefix.
# The backend mounts all routes under `/api` and allows CORS from http://localhost:3000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
